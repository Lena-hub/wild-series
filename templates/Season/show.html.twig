{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
<a href="{{ path('program_index') }}">
<div id="WS2" >
      <span id="W2">Wild</span>
      <span id="S2">Series</span>
</div>
</a>

    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ season.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('season_edit', {'id': season.id}) }}"><button class="save">editer</button></a>

    <a href="{{ path('season_index') }}"><button class="save">Retour à la liste</button></a>




    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
