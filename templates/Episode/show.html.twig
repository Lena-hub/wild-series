{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}

    <h1>Episode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ episode.title }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ episode.number }}</td>
            </tr>
        </tbody>
    </table>
       <a href="{{ path('episode_edit', {'id': episode.id}) }}"><button class="save">editer</button></a>

    <a href="{{ path('episode_index') }}"><button class="save">Retour à la liste</button></a>



    {{ include('episode/_delete_form.html.twig') }}
{% endblock %}
